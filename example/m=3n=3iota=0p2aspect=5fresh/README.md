# Original rotating-ellipse reference inputs

These authenticated vacuum inputs support input regeneration and regression checks for `../m=3n=3iota=0p2aspect=5finitebeta`. Their hashes are recorded in `inputs/manifest.json`. The separate vacuum campaign driver and results are not included here.
